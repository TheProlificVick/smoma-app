spring.config.import=optional:classpath:application-local.properties
jwt.secret=test-only-jwt-secret-key-must-be-32-chars-min
jwt.expiration=3600000
spring.ldap.urls=${SMOMA_LDAP_URL:ldap://127.0.0.1:389}
spring.ldap.base=${SMOMA_LDAP_BASE:dc=art,dc=cm}
spring.ldap.username=${SMOMA_LDAP_USERNAME:test}
spring.ldap.password=${SMOMA_LDAP_PASSWORD:test}
